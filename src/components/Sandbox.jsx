import React from 'react';
import PropTypes from 'prop-types';

Sandbox.propTypes = {
    
};

function Sandbox(props) {
    return (
        <div>
            
        </div>
    );
}

export default Sandbox;